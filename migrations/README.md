# Wira database migrations — historical reconstruction

## What this is

This directory is a **historical reconstruction** of the Wira production
database schema, built by reading every root-level `*.sql` file in this repo
(the ones that were previously copy-pasted by hand into the Supabase SQL
Editor, in no particular tracked order) and re-ordering their **exact,
unmodified SQL content** into numbered files that reflect the dependency
order they'd need to run in on a **fresh** database to arrive at (approximately)
the current live schema.

**Nothing here has been run.** No SQL in this directory (or anywhere else)
was executed against the live database as part of producing this
reconstruction. The live production database already has all of this
applied — you do **not** need to (and should not) replay migrations
0001–0015 against it.

Where the source files are genuinely redundant, conflicting, or depend on
something that doesn't appear to exist anywhere in this repo, that is called
out in the affected file's header comment rather than silently fixed —
per the ground rules for this reconstruction, **no SQL logic was invented,
rewritten, or "cleaned up"**, only reorganized, renamed, and split from the
real files.

## Going forward

New schema changes should be added here as new numbered files
(`0016_...sql`, `0017_...sql`, etc.), not as new ad-hoc scripts dropped in
the repo root and run by hand. Each new migration should:

- Get the next sequential number.
- Have a short header comment: what it does, and what it depends on.
- Contain real, reviewed SQL — not a copy-paste scratchpad.

The old root-level `*.sql` files (`master_schema.sql`, `setup_wallet.sql`,
etc.) were the source material for this reconstruction. They were removed
from the repo root on 2026-09-25, together with the one-off root-level
`*.js`/`*.py` scratch scripts: the numbered migrations in this folder are
the source of truth and are live. The old files remain available in git
history (e.g. `git log --all -- setup_wallet.sql`); the "Source" column
below and the list of excluded files further down refer to those
historical files.

## Log of new migrations added going forward

- **`0047_cancel_matched_ride_refund_rpc.sql`** (2026-09-19) — adds
  `wallet_refund_matched_ride(p_order_id, p_description)`, letting a
  customer or their assigned driver cancel an already-matched ride
  (`status IN ('accepted','picking_up')`) with a full refund to the
  customer's wallet if paid via WiraPay. Refused once `status = 'in_trip'`
  (trip physically underway) or later. See the file's header comment for
  the full cancellation-fee/eligibility policy reasoning.

- **`0048_driver_cancel_requeues_ride.sql`** (2026-09-19) — re-creates
  `wallet_refund_matched_ride()` from 0047 so a DRIVER-initiated
  cancellation re-opens the order (`status = 'pending'`, `driver_id = NULL`)
  instead of destroying it (`status = 'cancelled'`), closing 0047's own
  documented "known gap". Customer-initiated cancellation is unchanged
  (still full cancel + refund). The driver-cancel/requeue branch
  deliberately leaves `payment_status`/`wallet_balance` untouched — payment
  already happens up front at booking time, before any driver match, so a
  requeued order is simply returned to the exact paid-and-pending state it
  was already in before its first driver ever accepted; refunding here with
  no matching re-pay step anywhere in the accept flow would let a second
  driver complete an unpaid trip. See the file's header for the full
  reasoning, and `frontend-mitra/src/services/orderService.js`'s
  `subscribeToDriverOrders` (extended in the same commit) for how a
  requeued ride/send order reaches other drivers' realtime subscriptions,
  not just the 10s polling fallback.

- **`0049_send_service_pool_promos.sql`** (2026-09-19) — seeds one real,
  `service_type`-scoped promo code each for WiraSend (`SENDHEMAT`),
  WiraService (`SERVICEDISKON`), and WiraPool (`POOLBERSIH`), bringing them
  to parity with Ride/Food/Villa's existing promo codes now that
  SendPage.jsx/ServicePage.jsx/PoolPage.jsx/VillaPage.jsx all validate and
  redeem promos through the same `handleCheckPromo`/`increment_promo_usage`
  flow as RidePage.jsx/RestaurantPage.jsx.

`backend/database/schema.sql` and `backend/database/seed.sql` (both since
deleted from the repo, 2026-09-25) were **not** used as a source and were **not** modified. They describe a schema that
diverges significantly from what's actually live (separate `wallets` table,
`restaurants`/`menu_items` instead of `merchants`/`products`, a `drivers`
table with no matching `driver_profiles`, a `feature_flags` table keyed by a
UUID `id` instead of by `region`, etc.) — confirmed stale/fictional per this
task's background, and out of scope to touch.

## File listing (run order on a fresh database)

| # | File | Source | Purpose |
|---|------|--------|---------|
| 0001 | `0001_initial_core_schema.sql` | `master_schema.sql` | Core tables: `users`, `merchants`, `orders`, `messages`; base RLS policies; realtime for `orders`/`messages`; auth-sync safety net insert. |
| 0002 | `0002_merchants_rls_full_access.sql` | `fix_merchants_rls.sql` | Widens `merchants` RLS from read-only to full public access. |
| 0003 | `0003_feature_flags_table.sql` | `fix_feature_flags_rls.sql` | Creates `feature_flags` table (region-keyed) + RLS. Real origin of this table on the live DB. |
| 0004 | `0004_feature_flags_data_cleanup_and_realtime.sql` | `fix_feature_flags_data.sql` | Deletes a stale `feature_flags` row, enables realtime on the table. |
| 0005 | `0005_mitra_access_column.sql` | `migrate_mitra_access.sql` | Adds `mitra_access` JSONB column to `users`, backfills from `role` and from `merchants.owner_id`. |
| 0006 | `0006_users_rls_disable.sql` | `fix_users_rls.sql` | Disables RLS on `users` entirely (MVP admin-portal workaround). |
| 0007 | `0007_driver_profiles_table.sql` | `create_driver_profiles.sql` | Creates `driver_profiles` table (distinct from `drivers` — see 0014) + RLS. |
| 0008 | `0008_users_vehicle_columns_redundant.sql` | `update_users_table.sql` | Adds `vehicle_type`/`plate_number`/`specialization` to `users`. **Redundant** — already in 0001's tail; idempotent no-op if replayed. |
| 0009 | `0009_messages_table_redundant.sql` | `create_messages_table.sql` | Re-creates the `messages` table/RLS/realtime. **Duplicate** of the block already in 0001 — replaying it after 0001 on a fresh DB **will error** on `ALTER PUBLICATION ... ADD TABLE messages` (already a member). Skip when replaying end-to-end. |
| 0010 | `0010_seed_dummy_notifications_products.sql` | `seed_dummy_data.sql` | Creates `notifications` (`desc`/`read` columns) and `products` tables + seed rows. **Conflicts with 0011** — see warning in file header. |
| 0011 | `0011_seed_all_data.sql` | `seed_all_data.sql` | Creates `locations`, `promos`, `vehicles` tables; creates `notifications` (`description`/`is_read` columns — **conflicts with 0010**) and `products` (`+category_id` — **conflicts with 0010**); adds `merchants.price_per_night`/`description`; conditionally seeds merchants/products/villas. |
| 0012 | `0012_seed_operational_zones_flag.sql` | `seed_operational_zones.sql` | Inserts an `operational_zones` row into `feature_flags`; adds an admin UPDATE RLS policy. **Likely broken as written** — references a non-existent `is_active` column on `feature_flags`; see warning in file header. |
| 0013 | `0013_postgis_geofencing.sql` | `setup_geofencing.sql` | Enables `postgis`; creates `operational_zones` table + RLS; migrates the row from `feature_flags` into it; creates `get_zone_for_location()` RPC. |
| 0013b | `0013b_drivers_table_reconstructed.sql` | *(none — reconstructed from live schema, 2026-09-15)* | Creates `public.drivers` (id PK/FK -> `users.id`, vehicle_type, vehicle_plate, is_online, rating, status). Fills the gap below 0014 relies on. Not sourced from any root `.sql` file — see known issue #1 for how it was confirmed. |
| 0014 | `0014_postgis_nearest_driver.sql` | `setup_nearest_driver.sql` | Adds spatial columns/index/trigger to `public.drivers`; creates `get_nearest_drivers()`/`find_nearest_drivers()` RPCs, RLS, `driver_locations` view. Depends on 0013b now existing first. |
| 0015 | `0015_wallet_topup_system.sql` | `setup_wallet.sql` | Adds `users.wallet_balance`; creates `topup_requests` + `transactions` tables, RLS, unique-code trigger, and `approve_topup_request()`/`reject_topup_request()`/`cancel_topup_request()`/`get_pending_topup_codes()` RPCs. |
| 0016 | `0016_vehicles_pricing_admin.sql` | *(none — new feature, 2026-09-15)* | Adds `per_km_rate` to `vehicles`; admin INSERT/UPDATE/DELETE RLS policies (previously public-SELECT-only, no write policy existed at all). |
| 0017 | `0017_orders_pickup_coords.sql` | *(none — new feature, 2026-09-15)* | Adds real `pickup_lat`/`pickup_lng` columns to `orders` (previously only buried as JSON text in `details`, unusable by SQL/PostGIS). Stage 1 of upgrading driver matching from island-wide broadcast to distance-aware. |
| 0018 | `0018_nearby_pending_orders_rpc.sql` | *(none — new feature, 2026-09-15)* | Creates `get_nearby_pending_orders()` RPC (mirrors `get_nearest_drivers()` in the opposite direction) - scopes a driver's pending-orders list to a 15km radius, while always still showing orders with no pickup coordinates yet (send/service/pool). Stage 2 of driver matching. |
| 0019 | `0019_messages_rls.sql` | *(none — security fix, 2026-09-15)* | Enables RLS on `messages` (was enabled then immediately disabled in 0001/0009, no policies ever existed - any client could read/write any order's private chat). Scopes to the order's customer, assigned mitra, owning merchant, or admin. |
| 0020 | `0020_orders_dropoff_coords.sql` | *(none — new feature, 2026-09-15)* | Adds real `dropoff_lat`/`dropoff_lng` columns to `orders`, mirroring `pickup_lat`/`pickup_lng` from 0017. Powers GPS-assisted trip-stage confirmation in `DriverHomePage.jsx` (distance-to-target display, arrival radius highlight) - only `RidePage.jsx` populates these so far. |
| 0021 | `0021_fix_topup_description_text.sql` | *(none — text fix, 2026-09-15)* | Re-creates `approve_topup_request()` from 0015 unchanged except for the transaction `description` string: `'Top Up QRIS Statis DANA'` → `'Top Up QRIS Statis'`, since Wira's real QRIS (added this session, `frontend-user/src/assets/qris-wira.jpeg`) is a generic national QRIS (GPN), not DANA-specific. |
| 0022 | `0022_topup_rpc_auth_check.sql` | *(none — critical security fix, 2026-09-15)* | Adds a caller-must-be-admin check to `approve_topup_request()`/`reject_topup_request()`. Live, exploitable "print money" bug: both were `SECURITY DEFINER` with zero authorization check, so any anonymous unauthenticated caller could credit any wallet — empirically confirmed against the live DB before this fix. Was drafted-but-unapplied since `d181251` pending confirmation of real admin auth; confirmed safe to apply now (fake-admin-login fallback already removed from `frontend-admin`, real admin account with matching `public.users` role verified live). |
| 0023 | `0023_wallet_pay_transfer_rpcs.sql` | *(none — critical security fix, 2026-09-15)* | Adds `wallet_pay()`/`wallet_transfer()` `SECURITY DEFINER` RPCs and removes the client's direct INSERT policy on `transactions`. Fixes two more live holes: (1) any user could fabricate unlimited spendable balance via a direct `transactions` insert with arbitrary `type`/`amount`, since the display balance was summed client-side from that same open table; (2) `transfer()` only ever debited the sender and never credited any recipient — money silently vanished on every transfer. `users.wallet_balance` is now the single source of truth; `WalletContext.jsx` reads it directly and calls the new RPCs instead of inserting rows itself. Also tightens `transactions`' wide-open `USING (true)` admin policy and adds `amount > 0` / `wallet_balance >= 0` check constraints. |
| 0024 | `0024_tighten_orders_merchants_topups_rls.sql` | *(none — critical security fix, 2026-09-15)* | Replaces wide-open `USING (true)` policies on `orders`/`merchants`/`topup_requests` with ones scoped to every real call site across all three frontends. Documents the accepted guest-checkout tradeoff (anonymous orders with `user_id IS NULL` remain readable by anyone who knows the UUID) and flags a pre-existing `MerchantEarningsPage.jsx` bug (queries all `food` orders with no `merchant_id` filter) this migration surfaces but doesn't fix in SQL. |
| 0025 | `0025_users_rls_reenable.sql` | *(none — critical security fix, 2026-09-15)* | Re-enables RLS on `public.users`, disabled entirely since `0006` and never turned back on — confirmed live: anyone with just the anon key could read every user's name/phone/email/role/wallet_balance, no login required. New policies cover every real cross-user read mapped across all three frontends (mitra reading a customer's name on their own order, customer reading their assigned driver, admin full access, guest-checkout driver lookup) while closing the anonymous full-table hole. Adds a `list_technicians()` RPC replacing `ServicePage.jsx`'s anonymous `select('*')` over the whole table (RLS can't do column-level restriction, so that one site needed a code fix); also narrows `RidePage.jsx`/`SendPage.jsx`'s driver-lookup `select('*')` to just the columns they use. **Superseded by 0026 — see below, has a bug fixed there.** |
| 0026 | `0026_fix_users_rls_infinite_recursion.sql` | *(none — urgent bugfix, 2026-09-15)* | Fixes a bug in 0025 caught by live re-verification immediately after applying it: any RLS policy written directly on `public.users` that checked admin role via an inline `EXISTS (SELECT 1 FROM public.users WHERE id = auth.uid() AND role IN (...))` causes Postgres to detect infinite recursion the moment `users` has RLS enabled with that same policy shape — evaluating the subquery re-triggers the table's own policy, forever. This broke ALL access to `users` for everyone (not just the anon hole being closed) — signup, admin dashboard, and wallet balance display all started failing. Fixes it with a `SECURITY DEFINER` helper function `is_admin()` (bypasses RLS internally, same mechanism already proven safe by `approve_topup_request`/`wallet_pay`/`wallet_transfer`), and re-points every affected policy across 0023/0024/0025 at it. Also fixes a `varchar`/`text` return-type mismatch in `list_technicians()` (same class of bug as 0018's original one). |
| 0027 | `0027_drop_legacy_users_policies.sql` | *(none — urgent bugfix, 2026-09-15)* | Fixes a second bug in 0025, also caught by live re-verification: `0006`'s `ALTER TABLE ... DISABLE ROW LEVEL SECURITY` never dropped the original wide-open policies from `0001` (`"Allow users to read users" USING (true)`, etc.) — disabling RLS only makes existing policies inert, not gone. The moment 0025 re-enabled RLS, those three reactivated alongside the new scoped policies and, being OR'd together, fully undid the fix — confirmed live, anon could still read every row in `users` via a raw REST call after 0026. Drops all three by their original names; 0025/0026's own policies already cover insert/select/update correctly on their own. This dormant-policy issue is unique to `users` — every other table touched this session (`orders`/`merchants`/`topup_requests`/`transactions`) had RLS continuously enabled, so 0022-0024 already dropped every wide-open policy by exact name with no reactivation risk. |
| 0028 | `0028_mitra_payout_system.sql` | *(none — new feature, 2026-09-15)* | Adds a payout/withdrawal system for drivers, merchants, and technicians (every "Tarik Saldo" button in `frontend-mitra` was previously decorative, no `onClick` at all — and there was zero payout-destination data anywhere in the schema). 20% platform commission, mirrors `topup_requests` in reverse (`payout_requests` + `request_payout`/`cancel_payout_request`/`approve_payout_request`/`reject_payout_request` RPCs). Adds `orders.delivery_fee` and `users.payable_balance`, plus a trigger crediting `payable_balance` on order completion — for food orders (which now involve both a merchant and a driver on one `total_price`) the merchant earns from `total_price - delivery_fee` and the driver earns from `delivery_fee`, so the two shares never double-count the same money. Also updates the `orders` UPDATE policy so a driver can claim a `ready` food order (courier assignment — see the matching frontend-mitra changes in the same commit). |
| 0029 | `0029_products_rls_and_category.sql` | *(none — bugfix + new feature, 2026-09-15)* | `frontend-mitra`'s existing menu-management page (`MerchantMenuPage.jsx`) had no way to actually work: `products` has had SELECT+INSERT RLS since 0010/0011 but **no UPDATE or DELETE policy at all**, so every merchant's edit/delete action failed with "Akses ditolak" — while the INSERT policy was `WITH CHECK (true)`, letting anyone (including anonymous visitors) insert a product into any merchant's menu. Adds merchant-ownership-scoped INSERT/UPDATE/DELETE policies. Also adds a real `products.category` column — the menu form already had a category dropdown, but it was decorative (`category_id` from 0011 lost the 0010/0011 table-creation race, so the column never existed live). |
| 0030 | `0030_menu_image_storage.sql` | *(none — new feature, 2026-09-15)* | First Supabase Storage bucket in this project (`menu-images`, public-read). Every image field before this (`merchants.image`, `products.image`) was a plain URL text box with no real upload path anywhere. Adds owner-scoped INSERT/UPDATE/DELETE policies on `storage.objects` (path = `<uploader's auth uid>/<filename>`) so `MerchantMenuPage.jsx`'s "Tambah Menu Baru" form can upload a photo directly instead of requiring a pasted URL. |
| 0031 | `0031_courier_role_split.sql` | *(none — new feature, 2026-09-15)* | Extends the Restoran/Villa mitra_access split pattern to Ride/Send: adds `'courier'` as a new value a user's `mitra_access` (JSONB) can hold, alongside (not instead of) `'driver'`. Data-only backfill — every user who currently holds `'driver'` also gets `'courier'` appended in the same statement, so no currently-active driver's job queue shrinks when the split ships (a real driver can legitimately do both ride and send jobs with one motorbike, unlike a restaurant which can never also be a villa). New registrants choose one of Driver/Kurir at signup; either can self-activate the other later from Pengaturan Akun without a second admin review. No RLS policy changes — nothing on `orders`/`users` checks `mitra_access` value-by-value. |
| 0032 | `0032_fix_ready_orders_select_rls.sql` | *(none — critical bugfix, 2026-09-16)* | Found auditing the full WiraFood order-to-delivery flow: `0028`'s UPDATE policy let a driver claim a `ready` food order, but no SELECT policy was ever added to match — the live SELECT policy only exposed unclaimed orders while `status = 'pending'`. The moment a merchant marked an order `ready`, RLS silently hid it from every driver's query and from the realtime subscription alike, so a driver could never see it to claim it in the first place. This meant WiraFood's entire driver leg had been non-functional in production since `0028` shipped, regardless of how correct the frontend code was. Adds the missing `(status = 'ready' AND driver_id IS NULL AND merchant_id IS NOT NULL)` clause, mirroring the existing `'pending'` clause. |
| 0033 | `0033_unify_driver_job_preferences.sql` | *(none — new feature + intentional partial revert of 0031, 2026-09-16)* | Collapses `0031`'s Driver/Kurir portal split back into one unified Driver portal ("Option B" from a full options evaluation, approved by the product owner after using the split). Replaces the two-role-tag-per-portal model with a self-service preference model: adds `users.vehicle_type` (`'motor'`/`'mobil'`, repurposing the dead `0001`/`0008` column rather than the separately-populated, ride-matching-only `drivers.vehicle_type` from `0013b`/`0014` — see this migration's header for the full reasoning) and `users.job_type_preferences` (JSONB array, same shape as `mitra_access`) so each driver toggles Ride/Kurir/Makanan themselves, constrained by vehicle type, instead of the app assigning access via which login they used. Also adds `orders.package_size` (`'dokumen'`/`'kecil'`/`'sedang'`/`'besar'`), making WiraSend's package tier — previously only a customer-facing UI concept baked into a free-text `details` string — a real, queryable column, which is what makes "mobil can only carry large packages" actually enforceable. Backfills every currently-active driver-type account to `job_type_preferences = ["ride","send","food"]` / `vehicle_type = 'motor'` (exactly what they already effectively received before this shipped — zero disruption) and collapses every `'courier'` tag in `mitra_access` into `'driver'`. Adds `is_order_eligible_for_driver()` (the single SQL implementation of the eligibility rule — motor unrestricted by stored prefs, mobil excludes food unconditionally and restricts send to sedang/besar) and threads it into `get_nearby_pending_orders()` (return type changed to add `package_size`, so the function is dropped and recreated) — mirrors the equivalent single JS implementation in `frontend-mitra/src/services/orderService.js`'s `eligibleServiceTypesForDriver`/`isOrderEligibleForDriver`, used by both the fetch path and the realtime subscription path, so the rule can't drift between call sites the way it nearly did earlier tonight. |
| 0034 | `0034_driver_reviews.sql` | *(none — new feature, 2026-09-16)* | Creates `public.driver_reviews` (rating/comment per order, written by `RidePage.jsx`'s old inline post-trip rating UI). **Superseded by 0039+0041 — see below.** Confirmed live to contain 0 rows before it was superseded; table is left in place (unused) rather than dropped. **Numbering collision with `0034_orders_metadata.sql` below — see the dedicated note immediately under this table.** |
| 0034 | `0034_orders_metadata.sql` | *(none — new feature, 2026-09-16)* | One-line migration: adds `orders.metadata JSONB` for ad-hoc per-order structured data that doesn't warrant its own column yet. **Numbering collision — see note below.** |
| 0035 | `0035_support_tickets.sql` | *(none — new feature, 2026-09-16)* | Creates `support_tickets` (`user_id`/`subject`/`description`/`status` open\|in_progress\|resolved/`admin_response`) for the dispute/support system (`frontend-user`'s `SupportPage.jsx`, `frontend-admin`'s `SupportTicketsPage.jsx`). RLS as originally written: users can SELECT/INSERT their own tickets; a since-fixed `"Admins can view all tickets" USING (true)` policy (see 0043) that actually granted read access to every authenticated user, admin or not, not just admins; and a trigger calling `update_updated_at_column()`, a function that was never defined anywhere in this repo (see 0042) — confirmed live the trigger never actually bumps `updated_at` on UPDATE. No UPDATE policy existed at all, so admin ticket resolution in `SupportTicketsPage.jsx` silently affected 0 rows under RLS while showing a success toast (see 0043). |
| 0036 | `0036_update_promos.sql` | *(none — bugfix, 2026-09-16)* | Adds columns `promos` needed but never had (`type`, `discount`, `"validUntil"`, `usage`, `status`) and backfills real values onto the three seeded promo codes (`RIDE50`, `FOODFREE`, `VILLAKUTA`) that previously had no usable discount data. |
| 0037 | `0037_add_fcm_token.sql` | *(none — new feature, 2026-09-16)* | Adds `users.fcm_token TEXT` — storage for the Firebase Cloud Messaging device token, needed for the push-notification integration. |
| 0038 | `0038_saved_addresses_and_avatars.sql` | *(none — new feature, 2026-09-16)* | Adds `users.avatar_url`; creates `saved_addresses` (label/address/lat/lng/`is_primary`) with an owner-only `FOR ALL USING (auth.uid() = user_id)` policy; creates the first `avatars` Storage bucket (public-read) with owner-scoped (path-prefixed by uploader's uid) INSERT/UPDATE/DELETE policies on `storage.objects`, mirroring `0030`'s `menu-images` bucket pattern. |
| 0039 | `0039_reviews_and_tipping.sql` | *(none — new feature, 2026-09-16)* | Creates `public.reviews` (rating + optional tip per order) and `orders.is_reviewed`, plus `submit_review_and_tip()` RPC, written by the newer `ReviewModal.jsx`/`ActivityPage.jsx` flow. **Confirmed live as of 2026-09-18 to have never actually been applied** — calling `submit_review_and_tip` returns PGRST202 "function not found" — so this entire flow has been non-functional in production since it shipped. Still needs to be run by hand in the Supabase SQL Editor, followed by 0041 (see below) which replaces the RPC body before it's ever exercised live. |
| 0040 | `0040_wallet_refund_rpc.sql` | `add_refund_rpc.sql` (untracked root file — **confirmed live to have never been applied either**, same PGRST202 "function not found" as 0039) | Rewrites `wallet_refund()` to take `p_order_id` instead of a bare `p_amount`, verifying order ownership and a genuinely-refundable `status = 'pending'` state (`FOR UPDATE` row lock, mirroring `wallet_pay`/`wallet_transfer` from 0023) before crediting the wallet and flipping the order to `cancelled` in the same transaction — closes a double-refund hole and, since the old RPC was never live, also fixes the "Batalkan Pencarian" button being completely broken for every WiraPay customer today. |
| 0041 | `0041_refactor_review_tip_rpc.sql` | *(none — refactor + consolidation, 2026-09-18)* | Replaces `submit_review_and_tip()`'s hand-rolled debit logic with a call to `wallet_pay()` (removes a second, independent reimplementation of "debit a wallet safely"); keeps a direct, lock-ordered credit to the driver for the tip since `wallet_transfer()` resolves recipients by phone number, not user id. Also the frontend half of consolidating 0034's `driver_reviews` and 0039's `reviews` into one system — see this file's header for the full reasoning. |
| 0042 | `0042_update_updated_at_column_function.sql` | *(none — bugfix, 2026-09-18)* | Defines `update_updated_at_column()` (standard `NEW.updated_at = NOW(); RETURN NEW;` trigger function), confirmed missing from every `.sql` file in this repo by exhaustive grep, and confirmed missing live by testing: a real UPDATE on a real `support_tickets` row left `updated_at` unchanged. Re-creates `0035`'s `update_support_tickets_updated_at` trigger pointed at the now-real function. Scoped to `support_tickets` only — other tables with their own `updated_at` column are not touched here, to avoid scope creep beyond the bug being fixed. |
| 0043 | `0043_support_tickets_admin_rls_fix.sql` | *(none — critical security fix + bugfix, 2026-09-18)* | Two live-confirmed RLS bugs in `0035`, found with a real two-test-account before/after check (throwaway auth accounts, cleaned up after testing): (1) `"Admins can view all tickets" USING (true)` had no admin check at all — a real non-admin test customer could read a different test customer's private ticket (subject/description, plus phone via the `users` join `SupportTicketsPage.jsx` does) before this fix; replaced with an `is_admin()`-gated policy (reusing `0026`'s SECURITY DEFINER helper, not an inline `EXISTS` check, to avoid `0026`'s exact infinite-recursion failure mode on a policy that reads the same table it protects). (2) no UPDATE policy existed at all — a real test admin account, updating through the anon-key client exactly like `frontend-admin` does, got back `{ error: null, data: [] }` (0 rows affected), confirmed against a live re-read that the ticket was genuinely unchanged; adds a matching `is_admin()`-gated UPDATE policy. Paired with a `SupportTicketsPage.jsx` fix in the same commit that now chains `.select()` on the update and checks the returned row count before toasting success, so a future RLS regression here fails loudly instead of silently. |
| 0044 | `0044_drivers_realtime_publication.sql` | *(none — new feature/cheap fix, 2026-09-18)* | Adds `public.drivers` to the `supabase_realtime` publication so `RidePage.jsx`'s live driver-tracking map can actually receive `postgres_changes` events for driver position updates — every other realtime-dependent table (`orders`, `messages`, `feature_flags`) already has this, `drivers` never did in any tracked migration. **Could not be confirmed live either way** — checking `pg_publication_tables` needs a direct Postgres connection or an `exec_sql`-style RPC, neither of which exists in this repo (only PostgREST-exposed tables/RPCs are reachable via the service-role key; confirmed empirically, consistent with this file's own note above that `apply_sql.js` never executed DDL). Written as a `DO $$ ... EXCEPTION WHEN duplicate_object` block so it's a safe no-op if `drivers` turns out to already be there (e.g. added by hand via the dashboard). A human with SQL Editor access should run `SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime';` once to confirm the end state. |
| 0045 | `0045_topup_midtrans_method_and_amount_fix.sql` | *(none — critical money bugfix, 2026-09-19)* | Fixes three Midtrans top-up bugs found auditing `backend/routes/midtrans.js`: (1) `/charge` inserts `topup_requests` rows with `method: 'midtrans'`, but no `method` column ever existed on that table (created in 0015) — every Midtrans charge insert was failing at the DB level. Adds the column (default `'manual'`, matching every historical row). (2) 0015's `trg_topup_requests_unique_amount` trigger mutated `NEW.amount` for any round-number top-up by appending a random 101–999 "unique code", a scheme meant only for the manual QRIS-static bank-transfer flow (so an admin could visually match a transfer to a pending request) — applied to a Midtrans row, it meant Midtrans could charge the customer one amount while the wallet got credited a different (randomized) one. The trigger now skips the mutation entirely for `method = 'midtrans'` rows. Also narrows the partial unique index on `(amount) WHERE status = 'pending'` to exclude Midtrans rows, since two customers both topping up the same round amount (e.g. Rp 50.000) at once would otherwise hit a bogus unique-constraint violation — Midtrans rows are already uniquely identified by their own `order_id`, not by nominal. (3) The webhook's `transactions` insert has always set `reference_id` (the Midtrans `order_id`), but that column never existed either — confirmed live by actually POSTing a real, correctly-signed synthetic notification at a local backend instance: the wallet got credited and the request marked `approved`, then the ledger insert failed with PGRST204, silently leaving every real top-up un-audited. Adds the (nullable) column. |
| 0046 | `0046_promos_usage_limit_and_admin_rls.sql` | *(none — critical security fix + new feature, 2026-09-19)* | `public.promos` (0011/0036) had a `usage` column nothing ever incremented, no `usage_limit` column, and only a public SELECT policy — no INSERT/UPDATE/DELETE policy existed at all, so `frontend-admin/src/pages/PromosPage.jsx`'s create/edit/delete/toggle-status actions (previously calling undefined `savePromosToCloud`/`handleOpenAdd`/`handleOpenEdit`, now wired to real Supabase calls) would silently no-op under RLS even once the frontend bug was fixed. Adds a nullable `usage_limit` column (NULL = unlimited), an `is_admin()`-gated `FOR ALL` policy mirroring every other admin-managed table since 0026, and an `increment_promo_usage(promo_id)` RPC — a single `UPDATE ... WHERE status = 'Active' AND (usage_limit IS NULL OR usage < usage_limit)`, checking the affected row count via `GET DIAGNOSTICS`, so a promo can never be oversold by a race between two concurrent checkouts. Called from `RidePage.jsx`/`RestaurantPage.jsx` only after a promo-discounted order is actually created, not when a code is merely validated. Also seeds `WIRALOMBOK`/`DISKON10` as real rows (previously a hardcoded Rp 10.000-off string-match stub in `RestaurantPage.jsx`, bypassing this table entirely). |
| 0050 | `0050_users_merchants_column_grants_and_guardrails.sql` | *(none — critical security fix, 2026-09-19)* | `users_update`'s live RLS policy (0026) has no `WITH CHECK` at all, so any logged-in user could self-escalate `role`→`Superadmin` and inflate their own `wallet_balance`/`payable_balance` — same hole found on `users_insert`, closed here too. Fixes via `REVOKE UPDATE ON public.users FROM authenticated` + a narrow column `GRANT` (only the columns exhaustively grepped as genuinely self/admin-editable across all three frontends), plus a `BEFORE INSERT OR UPDATE` trigger (`enforce_users_write_guardrails`) as the only thing that can express "admin may touch this row, self may not" for `status`/`mitra_access`, which can't be fully blocked at the grant level since Supabase has no separate DB role for "admin" (admin dashboards write these columns on other users' rows as the same `authenticated` role a self-editing customer uses). Same column-grant technique applied to `public.merchants` (Fix 7) closing `merchants_update_owner_or_admin` letting a merchant self-set their own `rating`. |
| 0051 | `0051_orders_state_machine_trigger.sql` | *(none — critical security fix, 2026-09-19)* | `orders_insert_own`/`orders_update_mitra_or_admin` have no `WITH CHECK` restricting `status`/`total_price`/`payment_status`, and combined with `credit_payout_on_order_completed()` (0028) crediting `payable_balance` off `NEW.total_price` the instant `status` becomes `'completed'` with zero verification, this let any user either fabricate a brand-new already-"completed" order directly, or — worse — hijack any OTHER customer's real unclaimed-pending order straight to `completed` with an inflated `total_price` (the `(status='pending' AND driver_id IS NULL)` USING branch has no ownership check at all). Adds a `BEFORE INSERT OR UPDATE` trigger enforcing a real state machine: INSERT must be `status='pending'`/`payment_status IN ('unpaid','paid')` (both values confirmed live-legitimate by reading `OrderContext.jsx`'s one real insert call site); UPDATE to `status='completed'` requires `is_admin()` or the assigned `driver_id`/owning-merchant, never the customer; `total_price` is immutable after insert except for admins. Explicitly does NOT add server-side price verification at INSERT time — documented as a separate, larger follow-up. |
| 0052 | `0052_credit_wallet_balance_atomic_rpc.sql` | *(none — critical security fix, 2026-09-19)* | Adds `credit_wallet_balance_atomic(p_user_id UUID, p_amount NUMERIC)`, a `service_role`-only `SECURITY DEFINER` RPC doing a single atomic `UPDATE ... SET wallet_balance = wallet_balance + p_amount ... RETURNING`, replacing the non-atomic SELECT-then-UPDATE race in `backend/routes/midtrans.js`/`backend/routes/mutasiku.js`'s top-up webhooks (fixed in those files by a concurrent, separate change in the same session — confirmed both already call this RPC by this exact name). |
| 0053 | `0053_drivers_reviews_flags_notifications_hardening.sql` | *(none — critical/high/medium security fix, 2026-09-19)* | Four independent RLS fixes in one file: (1) drops `public.drivers`' `"Service role has full access to drivers"` policy — missing `TO service_role` meant it defaulted to PUBLIC, letting any anon-key client tamper with/delete any driver row (service_role already bypasses RLS via BYPASSRLS regardless, so the policy was redundant for its own stated purpose anyway). (2) Tightens `reviews`' INSERT policy to verify the order actually belongs to the caller and that `driver_id`/`merchant_id` match the order's real assignee, plus a `reviews_order_id_unique` constraint for race-free dedup (not a `WITH CHECK` subquery — a same-table correlated subquery here hits a real Postgres name-shadowing trap that would make the check a silent no-op, adjacent to the exact self-referencing-RLS-subquery bug class 0026 already had to fix once). (3) `operational_zones` management restricted to `is_admin()`; `feature_flags` writes split by `region` — `is_admin()` for everything except `region = 'mitra_registrations'`, which stays open to any `authenticated` user because it's a live, non-admin, self-service mitra-application queue (`UnauthorizedPage.jsx`/`RegisterPage.jsx`) that a blanket admin-only restriction would have broken; `notifications` SELECT/UPDATE/INSERT scoped to `auth.uid() = user_id` (the only real INSERT call site, `backend/services/notification.service.js`, uses the service-role client and is unaffected). (4) `submit_review_and_tip()` (0041) redefined with its initial order-row SELECT changed to `SELECT ... FOR UPDATE`, closing a race where two near-simultaneous calls could both pass the `is_reviewed` check before either committed. |
| 0054 | `0054_orders_driver_claim_guardrail.sql` | *(none — critical security fix, 2026-09-19)* | Found reviewing 0051 before handing it off: that migration correctly gated the `status → 'completed'` transition, but never restricted who may set `driver_id` on a still-unclaimed pending/ready order in the first place — the `(status='pending' AND driver_id IS NULL)` RLS branch has no ownership/role check at all. Any authenticated user could `UPDATE orders SET driver_id = self` on any unclaimed order (their own or someone else's), then complete it, since 0051's completion check only verifies you genuinely *are* `driver_id` by that point — a two-step route around 0051's fix that still mints `payable_balance`. Extends `enforce_orders_state_machine()` (same function, `CREATE OR REPLACE`) with three checks on the `driver_id: NULL → not NULL` transition: must be a self-claim (`NEW.driver_id = auth.uid()`), must never be the order's own customer claiming their own order as its driver, and the claimer must actually exist in `public.drivers` or have `'technician'` in `users.mitra_access` (the exact real-registry checks `get_nearest_drivers()`/`list_technicians()` already use) — closing both the self-dealing and order-hijacking variants of the same exploit. Live-verified end-to-end after 0050-0055 were applied: a real registered driver claiming and completing a genuine synthetic test order correctly got credited exactly 80% of `total_price` into `payable_balance`, while self-claim, self-dealing, hijack, and price-tamper attempts all failed — full details in the 0055 entry below. |
| 0055 | `0055_repair_0052_0053_permission_gaps.sql` | *(none — critical security fix, 2026-09-19)* | Live verification after the user applied 0050-0054 (real magic-link sessions, real RPC/table calls, checked by comparing actual DB state before/after rather than trusting an absence of a thrown error) found two of the intended restrictions had NOT actually taken effect live: (1) `credit_wallet_balance_atomic` (0052) — a plain authenticated test account successfully called it directly and credited its own `wallet_balance`, confirmed via a real before/after balance read (111813 → 112590 for a requested +777); reverted immediately after confirming — **this part is now confirmed fixed** (re-tested after applying 0055: `permission denied for function`, balance unchanged). (2) `feature_flags` (0053) — a plain authenticated test account successfully updated (and, re-tested further, even DELETEd) a real `region = 'features_config'` row, despite 0053/0055 intending `is_admin()` to be required — this part was **not** actually fixed by 0055; see 0056 for the real root cause. Re-issues the `REVOKE`/`GRANT` on `credit_wallet_balance_atomic` (from `PUBLIC`/`anon`/`authenticated`, to `service_role`) and re-asserts the 0053 `feature_flags` policies + `ENABLE ROW LEVEL SECURITY` (the latter turned out not to be the actual cause — see 0056). **Everything else from 0050-0054 was independently live-confirmed correct** in the same verification pass: users/merchants self-escalation blocked with legit self-fields still working (0050); fabricated-completed-order insert, price tampering, unauthorized claim, and self-dealing all blocked while the full legitimate claim→complete→payout flow (0051/0054) and `drivers`/`reviews`/`notifications`/`operational_zones` restrictions (0053) all still work correctly for their real, legitimate users. |
| 0056 | `0056_drop_untracked_feature_flags_policy.sql` | *(none — critical security fix, 2026-09-19)* | Root cause of 0055's still-open `feature_flags` gap: `SELECT policyname, cmd, qual, with_check FROM pg_policies WHERE tablename = 'feature_flags';`, run directly by the user in the Supabase SQL Editor (the only way to see this — `pg_policies` isn't reachable through PostgREST, so no migration-writing pass could have found it by grepping tracked `.sql` files or querying the REST API), revealed a policy named exactly `"Public feature_flags"` — `FOR ALL USING (true)`, no `WITH CHECK` — that matches no `CREATE POLICY` statement anywhere in 0001-0055. Almost certainly hand-created via the Supabase Studio UI at some point, entirely outside this migration history. Permissive RLS policies for the same command OR together, so this one `USING (true)` policy alone made every 0053/0055 restriction on this table irrelevant regardless of how correctly they were written. Drops it. Flags, as a general lesson: since this table had at least one untracked manually-created policy, other security-sensitive tables may too — a `pg_policies` spot-check across the rest of the schema is recommended, since it's the only way to catch this class of gap and no amount of grepping `.sql` migration history can substitute for it. |
| 0057 | `0057_pricing_rules_table.sql` | *(none — new feature, 2026-09-20)* | Of the six order-creation flows, only WiraRide had any DB-backed, admin-editable pricing (`public.vehicles`, 0011/0016, managed via `VehiclesPricingPage.jsx`) — WiraFood/WiraVilla are correctly merchant-owned (`products.price`/`merchants.price_per_night`), but WiraSend's package tiers, WiraService's category rates, WiraPool's service rates, and WiraFood's delivery-fee formula were all pure hardcoded JS constants with zero DB backing and zero admin control. Adds `public.pricing_rules` (`service_type, code, name, base_price, per_km_rate, is_active`), deliberately reusing `vehicles`' `(base_price, per_km_rate)` shape so a flat-fee tier is just a row with `per_km_rate = 0` and a distance-based one (food's delivery fee) uses both — same admin-editable-table pattern as `VehiclesPricingPage.jsx`, same admin-only write RLS. Seeded with the exact current hardcoded values from `SendPage.jsx`/`ServicePage.jsx`/`PoolPage.jsx`/`RestaurantPage.jsx` so live prices don't change until an admin edits them. |
| 0058 | `0058_orders_pricing_input_columns.sql` | *(none — new feature, 2026-09-20)* | Adds `rate_code`/`distance_meters`/`nights`/`promo_code` to `public.orders` — structured, machine-readable inputs 0059's price-computation trigger needs, replacing what previously only existed buried in free-text `details` or per-service-type-shaped `metadata`. All four nullable; this migration alone changes no behavior. |
| 0059 | `0059_orders_server_side_price_computation.sql` | *(none — critical money-integrity fix, 2026-09-20)* | Closes the gap 0051/0054 explicitly deferred: `orders.total_price` had been 100% client-supplied for every service type, driving real wallet debits and 20%-commission payouts off a number the browser controlled. Adds a `BEFORE INSERT` trigger (`enforce_orders_price_computation`) that recomputes `total_price` (and, for food, `delivery_fee`) from real data per service_type — `vehicles` (ride, by `rate_code` + `distance_meters`, replicating `RidePage.jsx`'s exact base+per-km formula), `pricing_rules` (send/service/pool, by `rate_code`), re-fetched `products.price` per cart line (food, via `metadata.items` — active immediately, no frontend change needed) plus `pricing_rules('food_delivery')` × `distance_meters`, and `merchants.price_per_night × nights` (villa). A shared `compute_promo_discount()` helper re-validates any `promo_code` against the real `promos` row (active/not expired/not exhausted) rather than trusting a client-computed discount amount. Confirmed with the user before writing: the server-computed price always wins over whatever the client sent — a stale client estimate silently gets corrected, it never hard-fails checkout — while an invalid `rate_code`/nonexistent product/missing villa price does reject the insert, since those can't come from real app usage. Graceful per-order transition: a service_type whose required structured column(s) are still NULL (pre-companion-frontend-deploy client) is left untouched rather than zeroed out, so protection activates incrementally as the paired frontend changes ship, not disruptively all at once. |
| 0060 | `0060_repair_missing_orders_metadata_column.sql` | *(none — bugfix, 2026-09-20)* | Live-verifying 0059's food price computation found `orders.metadata` genuinely does not exist on the live table (`42703 column does not exist`, confirmed not a PostgREST cache issue by retrying in isolation and auditing every other expected orders column individually — metadata was the only one missing) — `migrations/0034_orders_metadata.sql` was apparently never actually applied, despite being listed as applied in this README, the same class of gap this project has hit before (e.g. `wallet_refund()`, see 0038's entry). Since `RestaurantPage.jsx` has been sending `metadata` on every WiraFood order insert regardless (unrelated to this session's work), this means every real WiraFood checkout attempt has likely been failing outright — worth the user separately confirming whether WiraFood orders have actually been reaching customers. Idempotent re-run of 0034's own `ADD COLUMN IF NOT EXISTS`. |

| 0061 | `0061_add_accepted_at_to_orders.sql` | *(none — new feature, written by a separate AI agent session, 2026-09-21)* | Adds `orders.accepted_at` (set automatically by a trigger the moment `status` transitions to `'accepted'`) and redefines `wallet_refund_matched_ride` (0040/0047) to enforce a 3-minute cancellation window for customers after a driver accepts — drivers remain free to cancel any time before `in_trip`. Not written by the same session as 0050-0060; documented here retroactively since it was never added to this README when it landed. |
| 0062 | `0062_admin_wallet_correction_rpc.sql` | *(none — bugfix + critical money-integrity fix, 2026-09-22)* | Renumbered and hardened from an unapplied draft another AI agent session wrote as `"0056_admin_wallet_correction_rpc.sql"` — a real collision with the already-live `0056_drop_untracked_feature_flags_policy.sql`, caught before the draft was ever applied. Built in response to a real support case (a customer's QRIS top-up landed on an unrelated test account instead, because Mutasiku's nominal-based matching has no way to distinguish two different people's pending requests for the same amount — see `WIRA_HANDOFF.md` for the full story) — gives admins an `admin_correction_wallet_balance(p_user_id, p_amount, p_description)` RPC to manually credit/debit a wallet with a real audit trail. The original draft's RPC logic (admin-only via the established role-check pattern, atomic single `UPDATE...RETURNING`, negative-balance guard) was sound and is unchanged; what was fixed: (1) the original had the frontend insert the `transactions` ledger row as a SEPARATE call after the RPC — if that second call ever failed, the wallet would have silently moved with no audit trail at all, which is exactly the failure mode this whole feature exists to prevent. The ledger insert now happens inside the same `SECURITY DEFINER` function, one DB transaction, both commit or neither does. (2) The original stored corrections as `type: 'topup'`/`'payment'` "to keep the UI happy," even though `FinancePage.jsx`'s new ledger tab was already written expecting real `'correction_in'`/`'correction_out'` types (the mismatch was silently masked by an `amount > 0` fallback in that same display check) — now stores the real type. `frontend-admin/src/pages/UsersPage.jsx` updated to match (one RPC call instead of two, passes `p_description` through). |
| 0063 | `0063_anti_fraud_pin.sql` | *(none — new feature, written by a separate AI agent session, 2026-09-21, renumbered from `0040_anti_fraud_pin.sql` — collided with the long-established `0040_wallet_refund_rpc.sql`)* | Adds `orders.security_pin` (auto-generated random 4-digit code on insert) and `start_order_with_pin(p_order_id, p_pin_input)`, meant to let a driver confirm they're with the real passenger before a trip starts. **The original version of this RPC had no caller-authorization check at all** — fixed in 0066, see that entry. |
| 0064 | `0064_fix_pending_cancel.sql` | *(none — bugfix, written by a separate AI agent session, 2026-09-21, renumbered from `0041_fix_pending_cancel.sql` — collided with the long-established `0041_refactor_review_tip_rpc.sql`)* | Redefines `wallet_refund_matched_ride` to also allow cancelling a still-`pending` (no driver yet) order through this same function (0061's version had accidentally narrowed it to `accepted`/`picking_up` only) and adds the 3-minute-post-acceptance cancellation window for customers (drivers remain free to cancel any time before `in_trip`). **This version has a real payment_method typo (`'wirapay'` instead of the live `'wallet'`) that silently skips every real refund, and references `orders.accepted_at`, a column that was never actually applied to the database** — both fixed in 0066, see that entry. |
| 0065 | `0065_anti_fraud_hardening.sql` | *(none — security hardening, written by a separate AI agent session, 2026-09-21, renumbered from `0042_anti_fraud_hardening.sql` — collided with the long-established `0042_update_updated_at_column_function.sql`)* | Adds `orders.pin_attempts` and rate-limits `start_order_with_pin` to 5 tries before auto-cancelling the order (a reasonable idea, but see 0066 — without an ownership check this also let anyone who knew/guessed an order_id force-cancel a stranger's real trip by deliberately failing 5 times). Also extends `enforce_orders_state_machine` (0051/0054) to block ANY modification to an order once it's `cancelled`/`completed` (except by an admin) — a solid, unrelated general improvement, kept as-is. |
| 0066 | `0066_repair_pin_and_cancel_refund_bugs.sql` | *(none — critical bugfix + security fix, 2026-09-22)* | Live-testing 0061/0063-0065 (real synthetic matched wallet-paid order, real magic-link driver/customer sessions — not just reading the SQL) found three real, currently-live problems: (1) `orders.accepted_at` (0061) was never actually applied to the database despite 0063/0065's own new columns clearly having been — confirmed live: every call to `wallet_refund_matched_ride` fails outright with `column "accepted_at" does not exist`, meaning cancelling an already-matched ride is a complete functional outage in production right now, re-applies the column add. (2) `wallet_refund_matched_ride` (0064) checks `payment_method = 'wirapay'`, but the real value written everywhere else in this codebase (`OrderContext.jsx`, every earlier version of this same function) is `'wallet'` — had bug 1 not already made every call fail outright, this typo alone would have silently skipped every real refund while still reporting success to the customer; fixed to check the real value, and also restored the transaction `type` to `'refund'` (0064 had it as `'topup'`, same mislabeling class of bug fixed for admin corrections in 0062). (3) `start_order_with_pin` (0063, rate-limited in 0065) never checked that the caller was actually the order's assigned driver — confirmed the sole real call site is `frontend-mitra/src/pages/shared/ActiveOrderPage.jsx` (the assigned driver enters the customer-told PIN), so anyone who knew/guessed an order_id could attempt to start ANY order, and — worse, given 0065's own rate-limiting — could deliberately burn 5 failed attempts to force-cancel a stranger's real in-progress trip. Now requires `auth.uid() = driver_id` (or `is_admin()`) before anything else runs. |
| 0067 | `0067_scope_security_pin_and_chat_rework.sql` | *(none — critical security fix, 2026-09-22)* | Three parallel review agents auditing the anti-fraud PIN feature's frontend and a separate new "ephemeral chat" feature both found real privacy holes. (1) `orders.security_pin` rode along on every `SELECT *` a driver does to browse the dispatch/job-list feed (`orders_select_own_or_relevant`, 0032, deliberately grants broad SELECT on unassigned orders so drivers can see jobs to claim) — any driver, assigned or not, could read any order's PIN straight out of the ordinary job list or the realtime push, and even the legitimately-assigned driver's own client received it in `select('*')` responses despite the UI never displaying it to them, defeating the entire "customer tells the driver the code verbally" premise. A column-level `REVOKE` (0050's technique) isn't viable here since `orders` is read via `SELECT *` across a large number of already-working call sites and Postgres rejects the whole query, not just the forbidden column — instead moves the PIN into its own `order_security_pins` table with real RLS (readable only by the order's own customer, or the owning merchant for food orders — never the driver), auto-populated by an `AFTER INSERT` trigger, with `start_order_with_pin` (0066) updated to read from it. (2) The new ephemeral order chat used a plain (non-`private`) Supabase Realtime Broadcast channel keyed only by `chat_${orderId}` — no RLS applies to Broadcast, so anyone who learned an order id (which up to 10 dispatch-candidate drivers receive per order via push notification, whether or not they ever accept it) could join, read, and spoof messages in that order's chat indefinitely, reintroducing exactly the hole `0019_messages_rls.sql` closed for the *old* `messages` table via a different mechanism. Rather than build new Realtime Broadcast Authorization from scratch, the frontend fix (same commit) switches the chat to subscribe to Postgres Changes on that already-RLS-protected `messages` table instead (confirmed live: unrelated-order read returns 0 rows, unrelated-order insert is rejected by RLS) — no new chat-specific migration needed, this table and its policies already existed and already worked. |
| 0068 | `0068_restore_pin_table_lookup_after_reorder.sql` | *(none — regression fix, 2026-09-25)* | 0066 and 0067 both redefine `start_order_with_pin` via `CREATE OR REPLACE FUNCTION`, and were applied to the live database out of file-number order (0067 first, then 0066) — so 0066's older body, which reads `v_order.security_pin`, silently overwrote 0067's fix, which reads from the new `order_security_pins` table. Confirmed live: every `start_order_with_pin` call failed with `record "v_order" has no field "security_pin"` (that column was already dropped by 0067), a complete outage of the anti-fraud PIN flow in the opposite direction from what 0067 fixed. This migration re-applies 0067's function body byte-for-byte (no logic change) purely so the two migrations can no longer race — confirmed live afterward: correct PIN read from `order_security_pins` now succeeds and moves the order to `in_trip`. **Lesson for future migrations touching the same function across files: when two migrations both `CREATE OR REPLACE` the same function, the file that runs *last* wins regardless of its number — always re-verify live after any out-of-order apply, don't just trust the migration list.** |
| 0069 | `0069_fix_pin_trigger_missing_security_definer.sql` | *(none — critical outage fix, 2026-09-25)* | Discovered live-testing the RidePage.jsx booking flow during an unrelated UX fix pass: creating ANY order, as a real authenticated customer, failed 100% of the time with `new row violates row-level security policy for table "order_security_pins"` (Postgres 42501) — a full outage of order creation platform-wide. Root cause: 0067's `generate_order_pin()` AFTER INSERT trigger inserts into `order_security_pins`, and its own comment claims this is safe because the trigger "runs as table owner" — but the function was never actually declared `SECURITY DEFINER`, so it ran with the customer's own (invoker) privileges instead, and `order_security_pins` intentionally has no INSERT policy for authenticated users (only the trigger is supposed to write to it). Without `SECURITY DEFINER`, RLS correctly rejected the trigger's own insert, aborting the entire enclosing `orders` INSERT. Fixed by adding `SECURITY DEFINER` to `generate_order_pin()`, matching what 0067 always claimed was already true. Confirmed live via an isolated diagnostic (authenticated customer session, not service role): order creation failed before this migration, 100% reproducible regardless of service type. **This means every order created anywhere in the app between 0067 landing and this migration would have failed at the database level** — worth checking with the user whether any customers reported "gagal membuat pesanan" errors in that window. |
| 0070 | `0070_atomic_wallet_checkout.sql` | *(none — critical money-integrity fix, 2026-09-25)* | Closes a wallet-minting exploit. WiraPay orders were created in two unrelated client steps (`wallet_pay(client amount)` then `INSERT orders (..., payment_method='wallet', payment_status='paid')`), and nothing checked the first step happened — so a customer could INSERT a wallet/paid order directly (or simply pick "Transfer Bank" on Pool/Villa, which `OrderContext` also mapped to wallet/paid without charging) and cancel it for a refund of money never paid. The pending-order UPDATE policy (0028) additionally let any user rewrite `user_id`/`payment_method`/`payment_status` on any unassigned pending order. Adds `create_order_and_pay(...)` (SECURITY INVOKER so RLS and 0059's price trigger still apply) + `charge_wallet_for_order(...)` (SECURITY DEFINER): order insert, debit of the exact server-computed `total_price`, ledger row and `payment_status='paid'` in one transaction. `enforce_orders_state_machine` now rejects `payment_status <> 'unpaid'` on client INSERT, rejects `payment_method='wallet'` outside `create_order_and_pay`, and freezes ownership/payment/pricing columns on non-admin client UPDATEs (every real client update only touches `status`/`driver_id`). **Must be applied before/with the companion frontend deploy** (OrderContext.addOrder + the six checkout pages). |
| 0071 | `0071_atomic_webhook_topup_approval.sql` | *(none — money-integrity fix, 2026-09-25)* | Both top-up webhooks (`backend/routes/midtrans.js`, `backend/routes/mutasiku.js`) approved a request in three separate calls (flip `topup_requests.status` to `approved`, `credit_wallet_balance_atomic`, insert the `transactions` row). If the credit or ledger call failed after the status flip committed, the gateway's retry saw a non-pending row and skipped it, so the customer paid but was never credited. Adds `approve_topup_and_credit(...)` (SECURITY DEFINER, **service_role only**) doing all three in one transaction under a row lock, re-checking amount and method; returns `'approved'`/`'already_processed'`/`'not_found'` so replays stay 200 no-ops. The admin manual path (`approve_topup_request`, 0022) was already atomic and is unchanged. **Apply before deploying the companion backend change**; the old backend keeps working after it's applied. |
| 0072 | `0072_server_side_dispatch.sql` | *(none — reliability fix, 2026-09-25)* | Sequential driver dispatch (ping one nearby driver every 15s) used to run only inside the customer's open browser tab (`frontend-user/src/hooks/useOrderDispatch.js`), so closing the app, locking the phone or losing the tab stopped all pings and the order sat in 'searching' forever. Moves it server-side: `order_dispatch_pings` log table (RLS on, no policies), `dispatch_due_orders(p_limit)` (pending + unassigned + ride/send/pool/service + created in the last 30 min + no ping in the last 15s) and `dispatch_next_ping(p_order_id)` (locks the order row, picks the nearest not-yet-pinged candidate via `get_nearest_drivers` — filtered by the order's `rate_code` vehicle type for rides — or `list_technicians` for pool/service, logs it, restarts the round when everyone was pinged). All service_role only. Called by `backend/routes/dispatch.routes.js`, which sends the push with server-built text. The row lock + log make it one ping per order per 15s regardless of how many triggers fire. **Apply before deploying the companion backend/frontend change** — the new frontend asks the backend to dispatch, and the backend needs these functions. |
| 0073 | `0073_dispatch_cron_schedule.sql` | *(none — reliability fix, 2026-09-25)* | Schedules `wira-dispatch-tick` with pg_cron every 15 seconds: `net.http_post` to `https://wira-backend-seven.vercel.app/api/dispatch/tick` with header `x-dispatch-secret` read from Supabase Vault (`dispatch_cron_secret`), and only when `dispatch_due_orders(1)` returns something, so idle ticks make no HTTP call. Requires, once and outside git: `vault.create_secret(...)` for `dispatch_cron_secret` and the same value in the wira-backend Vercel env var `DISPATCH_CRON_SECRET` (then redeploy). Can be applied after the 0072 deploy — until then the customer's open app still drives dispatch through the same server endpoint. |
| 0074 | `0074_orders_require_login.sql` | *(none — security fix, 2026-09-25)* | 0024's `orders_insert_own` allowed `auth.uid() IS NULL AND user_id IS NULL` ("guest checkout"), so anyone with the public anon key could INSERT pending orders with no account. Those rows appear in every driver's job feed and are picked up by server-side dispatch (0072/0073), which pushes real notifications to real drivers — a spam/harassment channel (no money: 0070 forces them unpaid). Guest checkout was never reachable in the app (every order page is under `Layout.jsx`, which redirects to `/login`); the leftover guest branch in `OrderContext.addOrder` and `ecosystemService.createEcosystemOrder` are removed in the same commit. Recreates `orders_insert_own` as `WITH CHECK (auth.uid() IS NOT NULL AND auth.uid() = user_id)` and `REVOKE INSERT ON public.orders FROM anon` (the grant only came from Supabase defaults; no migration, trigger or RPC relies on it — `create_order_and_pay` is authenticated-only). Existing `user_id IS NULL` rows and anon SELECT/UPDATE are untouched. **Must be applied in Supabase**; safe before or after the frontend deploy (the logged-in path is unchanged). |
| 0075 | `0075_cash_orders_commission_debt.sql` | *(none — money-integrity fix, 2026-09-25)* | `credit_payout_on_order_completed` (0028) credited the 80% mitra share on every completed order, including `payment_method='cash'` (Tunai) where the driver — or, with no driver (villa), the merchant — already collected the whole `total_price` in hand, so the mitra kept 100% of the cash and could still withdraw 80% more. Recreates the trigger function: shares are credited exactly as before, then for cash orders only the collector (`driver_id`, else the merchant owner) is debited the full `total_price`. Net: cash ride/send/pool/service driver −20%; cash food with courier merchant +80% of food, driver −(total − 80% of delivery fee); cash villa merchant −20%. `payable_balance` may now go negative, so 0028's `users_payable_balance_nonneg` CHECK is dropped (without that the debit would abort order completion). `request_payout` (0028, never redefined) already refuses `amount > payable_balance`, so a negative balance blocks withdrawals. `wallet`/`transfer` unchanged — `transfer` has no defined recipient account yet and needs a product decision. **Must be applied in Supabase**; apply together with the companion frontend-mitra deploy (the earnings screens now show the cash deduction, which is only true once this is applied). |
| 0076 | `0076_server_side_promo_usage.sql` | *(none — security + integrity fix, 2026-09-25)* | Promo usage was counted by the browser after the order was created: the six checkout pages called `increment_promo_usage(promo_id)` (0046, granted to every authenticated user) fire-and-forget. Anyone logged in could loop that RPC to exhaust any promo's `usage_limit` for everyone, one account could reuse a code on unlimited orders, and a failed call / closed tab never counted the use. Adds `promos.per_user_limit` (NULL = unlimited, existing promos unchanged; CHECK > 0) and a BEFORE INSERT trigger `trg_orders_promo_usage` (SECURITY INVOKER, gated exactly like 0059's price trigger, so it runs for plain client INSERTs and `create_order_and_pay` alike, in the INSERT's own transaction) that calls `consume_promo_for_order` (SECURITY DEFINER, refuses to run outside a trigger via `pg_trigger_depth()`): one guarded `UPDATE ... usage < usage_limit` under the promo row lock (so two concurrent orders can't both take the last slot), then the user's non-cancelled orders with that code are counted against `per_user_limit`. Exhausted / per-user limit reached **raises** (`Kuota promo ... sudah habis` / `... sudah Anda gunakan ...`) and the order is not created, rather than silently charging full price — `addOrder` and every checkout page toast `err.message`. Unknown/expired/wrong-service codes still silently give no discount (0059). `increment_promo_usage` is revoked from `PUBLIC`/`anon`/`authenticated` (kept for `service_role`). Usage is still not given back on cancel; cancelled orders don't count toward `per_user_limit`. **Apply BEFORE the companion frontend deploy** (pages stop calling the RPC; admin `PromosPage` saves `per_user_limit`): the old frontend's leftover RPC call just gets "permission denied", which it only logs, so no double count; the new frontend without 0076 counts nothing and the admin save fails. |
| 0077 | `0077_qris_order_checkout.sql` | *(none — feature, 2026-09-25)* | Pool/Villa **pay directly with the static QRIS** (replaces the old "Transfer Bank", which had no recipient account or confirmation). New `create_order_awaiting_qris(...)` (SECURITY INVOKER, same pricing params as `create_order_and_pay` minus the description; pool/send/service/villa only) inserts a normal WiraPay order (`payment_method='wallet'`, `unpaid`) whose status the new last-named BEFORE INSERT trigger `trg_zz_orders_await_qris` rewrites to `awaiting_payment` (only when the RPC's transaction-local `wira.qris_checkout` flag is on, after 0070's state machine accepted `pending`), then inserts a pending manual `topup_requests` row for `CEIL(total/1000)*1000` linked by the new `topup_requests.order_id` column, so 0045's trigger adds the 101–999 unique code on top. Returns `{order, qris_amount}`. Every mitra list, realtime handler and the dispatch cron only look at `pending`, so the unpaid order is invisible to mitra. **Apply together with 0078, before the frontend deploy.** |
| 0078 | `0078_qris_order_payment.sql` | *(none — feature, 2026-09-25)* | Deferred constraint trigger `trg_pay_order_after_qris_topup` on `topup_requests`: when a request linked to an order becomes `approved` (Mutasiku webhook via 0071, or admin via 0022 — both flip status before crediting, hence DEFERRED to commit), debits the order's `total_price` from the just-credited wallet, writes the `payment` ledger row and moves the order to `pending`/`paid` (it then dispatches like any WiraPay order). Never raises: if the order was cancelled/expired first, the money simply stays in the wallet. `trg_guard_awaiting_payment_orders`: clients may only cancel an `awaiting_payment` order (no accept/claim/other status), and may never move an order *into* `awaiting_payment`. `cancel_awaiting_qris_order(order_id)` for the customer. `expire_awaiting_qris_orders()` + pg_cron job `wira-expire-qris-orders` (every minute): unpaid after 15 min → cancelled; linked pending requests older than the webhook's 2-hour match window → cancelled (frees the unique nominal). Verified on local PostgreSQL 16 with the real 0045/0059/0070/0071/0076 bodies. Companion backend change: `routes/mutasiku.js` pushes the villa owner once a linked villa order is paid. |
| 0079 | `0079_close_anon_data_leaks.sql` | *(none — security fix, 2026-09-25)* | Confirmed live with only the public anon key: (a) `users_select` (0026) returned the **full row** (email, phone, fcm_token, wallet/payable balance) of every driver who served a guest order, via its `auth.uid() IS NULL` guest branch; (b) `orders_select_own_or_relevant` (0032) returned guest orders and **every unassigned pending/ready order of real customers** (coordinates, details) because the job-feed branches never required a login, and `orders_update_mitra_or_admin` (0028) let anon edit e.g. `title`/`details` of those orders; (c) 0015's "Anyone can check pending amounts" exposed `user_id`/amount/`order_id` of every pending top-up. Recreates the three policies without the guest branch and with `auth.uid() IS NOT NULL` on the job-feed branches (every logged-in branch unchanged), drops (c) (unique codes are still checked via `get_pending_topup_codes`, amounts only, and 0045 re-rolls collisions), and revokes anon's default SELECT/INSERT/UPDATE/DELETE on `users`, `orders`, `topup_requests`. Guest checkout is gone since 0074, so no app flow is affected; no frontend change needed. Verified on local PostgreSQL 16 before/after with anon, customer, driver, merchant owner and admin sessions. Known remaining exposure, not fixed here: a logged-in counterparty (customer ↔ their driver, merchant ↔ their customer) can still read the other's full `users` row. |
| 0080 | `0080_counterparty_profiles.sql` | *(none — privacy fix, 2026-09-25)* | Follow-up to 0079 for logged-in users. `users_select` (0026) let a customer read the **full row** of every driver they ever had (and a driver/merchant owner every customer's): email, fcm_token, wallet/payable balance, mitra_access — the apps only show name/phone/vehicle. `drivers` "Public can view online active drivers" (0014) was `USING (true)`: anyone, anon included, read every driver's live lat/lng and plate; `driver_locations` exposed name + location to anon. Now `users_select` = own row or admin; `drivers` SELECT = own row, admin, or the driver of the caller's own not-yet-finished order (keeps ActiveOrderPage's location fetch + realtime); new `get_counterparty_profiles(uuid[])` (SECURITY DEFINER, authenticated only) returns only `id, name, phone, vehicle_type` under the same who-may-see-whom rule `users_select` had (plus reviewers of the calling driver). Revokes anon on `drivers`, `driver_locations` (also authenticated — unused), and `get_nearest_drivers`/`find_nearest_drivers` (logged-in RidePage keeps them). Companion frontend change: user ActiveOrderPage and mitra ActiveOrderPage/DriverHomePage/TechOrdersPage/MerchantOrdersPage/DriverProfilePage use `services/profileService.js`, which falls back to the old direct read while the RPC is missing. **Deploy the frontend first, then apply.** Verified on local PostgreSQL 16 before/after with customer, driver, merchant owner, admin and anon sessions. |
| 0081 | `0081_expire_stale_manual_topups.sql` | *(none — reliability fix, 2026-09-26)* | Manual (static QRIS) top-up requests never expired: the Mutasiku webhook (`routes/mutasiku.js`) only matches pending manual requests from the last 2 hours, but abandoned ones stayed `pending` forever, kept their unique nominal (0045's partial unique index) and still showed as pending in the wallet. 0078's `expire_awaiting_qris_orders()` only cancelled order-linked ones. Redefines it (same signature, SECURITY DEFINER, same REVOKEs; the existing pg_cron job `wira-expire-qris-orders` keeps calling it every minute): the order expiry is unchanged, and **every** pending request with `method IS DISTINCT FROM 'midtrans'` older than 2 hours → `cancelled`. Midtrans requests are never touched. Companion frontend change: `topupService.fetchUserTopUpRequests` hides pending requests older than 2 hours, and the WalletPage QRIS step says the payment must arrive within 2 hours. Safe to apply before or after the frontend deploy. Verified on local PostgreSQL 16 (old manual / NULL-method / order-linked cancelled; old midtrans, fresh manual and approved untouched; re-apply clean). |

## Duplicate migration number: two files are both `0034`

`0034_driver_reviews.sql` and `0034_orders_metadata.sql` are unrelated files
that both landed on the number `0034`. Neither has been renamed — doing so
would misrepresent the historical record of what was actually run against
the live DB under that filename. Both need to be run; their content has no
interdependency (one creates a standalone `driver_reviews` table now
superseded by `0039`'s `reviews` table, the other adds a single nullable
JSONB column to `orders`), so they can be run in either order relative to
each other. Just make sure both get run — it's easy to see one `0034` file,
run it, and assume the number is fully accounted for.

## Files intentionally excluded from the numbered sequence

These were read and cataloged but not turned into migrations, because they
are not schema-altering in a way worth replaying, or are pure test/diagnostic
artifacts (all removed from the repo root on 2026-09-25; see git history):

- **`test_realtime.sql`** — a single `ALTER PUBLICATION supabase_realtime ADD TABLE orders;` statement, already covered by 0001. Replaying it after 0001 would error (table already a publication member).
- **`test_insert_mock.sql`** — a single test `INSERT` of one mock driver user row, not real seed data.
- **`check_users_rls.sql`** — a read-only `SELECT` diagnostic query against `pg_tables`; no schema/data effect at all.
- **`generate_sql.mjs`** — a Node script that *generates* SQL to `seed_all_data.sql`; it was never itself executed against the database, and its current content has drifted from what `seed_all_data.sql` actually contains (evidence that `seed_all_data.sql` was hand-edited after being generated).
- **All other root-level `*.js` files** (`apply_sql.js`, `fix_admin.js`, `patch_*.js`, `rollback_gps.js`, `test_*.js`, etc.) — inspected individually. `apply_sql.js` is an inert stub that never executes DDL (its own comment says it can't). The rest are frontend `.jsx` source-code patchers or Supabase-client-level data/verification scripts (row inserts/updates through the REST API, GPS error-message rewrites, UI patches) — none contain raw schema-altering SQL.

## Known issues to resolve with a human who has DB access

1. **RESOLVED (2026-09-15).** ~~Missing `public.drivers` table~~ — confirmed live via `information_schema.columns` + `table_constraints`: `id UUID PRIMARY KEY REFERENCES users(id)`, `vehicle_type`, `vehicle_plate`, `is_online`, `rating`, `status` (plus `lat`/`lng`/`location`/`updated_at` added later by 0014 itself). It genuinely was never captured in any root `.sql` file — most likely created by hand via the Table Editor. Reconstructed as `0013b_drivers_table_reconstructed.sql`, built from the live schema, not from `backend/database/schema.sql`.

2. **RESOLVED (2026-09-15).** ~~`notifications`/`products` schema conflict~~ — confirmed live via `information_schema.columns`: `notifications` has `description`/`is_read` (i.e. `seed_all_data.sql` / 0011's shape won), while `products` has **no** `category_id` (i.e. `seed_dummy_data.sql` / 0010's shape won). Split result — neither file fully "won"; each table independently ended up matching a different source file. No further action needed since this only documents historical fact, but if either 0010 or 0011 is ever replayed against a fresh DB, expect the losing file's inserts for these two tables to fail exactly like they silently did in production.

3. **RESOLVED (2026-09-15).** ~~`seed_operational_zones.sql` (0012) references non-existent `is_active`~~ — confirmed live: `feature_flags` only has `(id, region, features, updated_at)`, no `is_active`. 0012's insert would indeed fail as written. Since 0013 already migrates the zones data out of `feature_flags` into a dedicated `operational_zones` table, this is dead code going forward — no fix needed unless 0012 is ever replayed standalone.

4. **Duplicate `messages` table creation (0001 vs 0009).** `create_messages_table.sql` (0009) is a byte-for-byte duplicate of the "TABEL CHAT" block already appended to the end of `master_schema.sql` (0001). Harmless as two separate historical files, but not both replayable back-to-back on a fresh DB (see 0009's header).

5. **Git commit history for these files does not reliably reflect real chronological order** — many of the source files share identical first-commit timestamps (e.g. everything touched around `2026-09-12 20:37:45`), consistent with a bulk/batched import rather than incremental commits as each file was actually written and run. Ordering in this `migrations/` folder was determined primarily from **logical dependency** (a table must exist before it's altered) and secondarily from **on-disk file modification times** (which vary at minute/second granularity and are more informative here), not from `git log` dates. `git log` dates are still recorded in each file's header per the task instructions, but should be read as "when this file entered version control," not "when it ran against the database."
